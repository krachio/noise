language: rust (workspace) + python
type_checker: cargo clippy --workspace -- -D warnings
test_runner: cargo test --workspace
package_manager: cargo (workspace); uv (per Python subproject)
pre_commit_hooks:
