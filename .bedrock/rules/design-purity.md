No backward compatibility shims. No half-baked solutions.

If old code stands in the way of the right design, delete it. Don't wrap it, don't deprecate it, don't add a compatibility layer. Break imports if needed — the right structure wins.

Either do something right or don't do it. No "fix later" TODOs, no "good enough for now" compromises, no transitional code that outlives its transition.
