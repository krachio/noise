repos:
  - repo: local
    hooks:
      - id: pyright
        name: pyright (strict)
        language: system
        entry: uv run pyright
        types: [python]
        pass_filenames: false
      - id: pytest
        name: pytest
        language: system
        entry: uv run pytest
        pass_filenames: false
        always_run: true
